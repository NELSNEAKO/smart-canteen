import React from "react";
import "./Spinner.css"; // Ensure you import the CSS file

const Spinner = () => {
  return (
    <div className="verify">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
